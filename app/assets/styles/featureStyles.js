import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    holder: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1
    },
    img: {
        width: 50,
        height: 50
    },
    text:{
        textAlign: 'center',
        marginTop: 10,
        color: "#32AAEA"
    }
});
export default styles